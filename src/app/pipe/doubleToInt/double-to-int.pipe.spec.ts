import { DoubleToIntPipe } from './double-to-int.pipe';

describe('DoubleToIntPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleToIntPipe();
    expect(pipe).toBeTruthy();
  });
});
