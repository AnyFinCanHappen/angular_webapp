import { MinuteFormatPipe } from './minute-format.pipe';

describe('MinuteFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
