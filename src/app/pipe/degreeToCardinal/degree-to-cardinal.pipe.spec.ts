import { DegreeToCardinalPipe } from './degree-to-cardinal.pipe';

describe('DegreeToCardinalPipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeToCardinalPipe();
    expect(pipe).toBeTruthy();
  });
});
