import { HourFormat2Pipe } from './hour-format2.pipe';

describe('HourFormat2Pipe', () => {
  it('create an instance', () => {
    const pipe = new HourFormat2Pipe();
    expect(pipe).toBeTruthy();
  });
});
